// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false /*,
  api_url: "https://wurs0fovfi.execute-api.eu-west-2.amazonaws.com/prod/",
  broadcast_url: "wss://2lbltck2s5.execute-api.eu-west-2.amazonaws.com/dev",
  login_url: "https://serverless-chat-users.auth.eu-west-2.amazoncognito.com/login?client_id=7e2e1q6oekrvsd05ec4fmsufih&response_type=token&redirect_uri=http://localhost:4200/callback"
*/
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
